import React from 'react';

class Button extends React.Component {
    render() {
        return (
            <React.Fragment>
                <button>

                </button>
            </React.Fragment>
        );
    }
}

export default Button;
